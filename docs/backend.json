
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user in the FocusFinds application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        },
        "username": {
          "type": "string",
          "description": "User's username or display name."
        },
        "profileImageUrl": {
          "type": "string",
          "description": "URL of the user's profile image.",
          "format": "uri"
        },
        "isAdmin": {
          "type": "boolean",
          "description": "Indicates whether the user has administrative privileges."
        }
      },
      "required": [
        "id",
        "email",
        "username"
      ]
    },
    "Media": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Media",
      "type": "object",
      "description": "Represents an image or video on the platform.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the media."
        },
        "title": {
          "type": "string",
          "description": "Title of the media."
        },
        "description": {
          "type": "string",
          "description": "Description of the media."
        },
        "mediaUrl": {
          "type": "string",
          "description": "URL of the original media file.",
          "format": "uri"
        },
        "thumbnailUrl": {
          "type": "string",
          "description": "URL of the media thumbnail (especially for videos).",
          "format": "uri"
        },
        "mediaType": {
          "type": "string",
          "description": "The type of media.",
          "enum": ["image", "video"]
        },
        "uploadDate": {
          "type": "string",
          "description": "Date and time when the media was uploaded.",
          "format": "date-time"
        },
        "dominantColor": {
          "type": "string",
          "description": "The dominant color of an image, used for UI theming."
        }
      },
      "required": [
        "id",
        "title",
        "mediaUrl",
        "mediaType",
        "uploadDate"
      ]
    },
    "SiteSettings": {
      "title": "SiteSettings",
      "description": "A singleton document to store site-wide settings.",
      "type": "object",
      "properties": {
        "heroImageUrl": {
          "type": "string",
          "description": "The URL for the homepage hero image background.",
          "format": "uri"
        },
         "heroImageHint": {
          "type": "string",
          "description": "The AI hint for the hero image."
        }
      }
    },
    "Message": {
      "title": "Message",
      "description": "Represents a message sent from a user to the admin.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the sender."
        },
        "email": {
          "type": "string",
          "description": "Email of the sender (if logged in).",
          "format": "email"
        },
        "userId": {
            "type": "string",
            "description": "User ID of the sender (if logged in)."
        },
        "message": {
          "type": "string",
          "description": "The content of the message."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the message was sent.",
          "format": "date-time"
        },
        "isRead": {
          "type": "boolean",
          "description": "Indicates if the admin has read the message."
        }
      },
      "required": ["name", "message", "createdAt"]
    },
    "Reply": {
      "title": "Reply",
      "description": "Represents a reply within a message thread.",
      "type": "object",
      "properties": {
        "message": {
          "type": "string",
          "description": "The content of the reply."
        },
        "sentAt": {
          "type": "string",
          "description": "Timestamp when the reply was sent.",
          "format": "date-time"
        },
        "isFromAdmin": {
          "type": "boolean",
          "description": "True if the reply is from an admin."
        }
      },
      "required": ["message", "sentAt"]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profile information. Accessible only to the user and administrators.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/media/{mediaId}",
        "definition": {
          "entityName": "Media",
          "schema": {
            "$ref": "#/backend/entities/Media"
          },
          "description": "Stores media (image/video) metadata.",
          "params": [
            {
              "name": "mediaId",
              "description": "The unique identifier of the media."
            }
          ]
        }
      },
      {
        "path": "/roles_admin/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores administrator roles. Existence of a document grants admin privileges. DBAC - Existence over Content.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
       {
        "path": "/settings/main",
        "definition": {
          "entityName": "SiteSettings",
          "schema": {
            "$ref": "#/backend/entities/SiteSettings"
          },
          "description": "A singleton document for global site settings like the hero image.",
          "params": []
        }
      },
      {
        "path": "/messages/{messageId}",
        "definition": {
          "entityName": "Message",
          "schema": {
            "$ref": "#/backend/entities/Message"
          },
          "description": "Stores messages sent by users to the site admin.",
           "params": [
            {
              "name": "messageId",
              "description": "The unique identifier of the message."
            }
          ]
        }
      },
      {
        "path": "/messages/{messageId}/replies/{replyId}",
        "definition": {
          "entityName": "Reply",
          "schema": {
            "$ref": "#/backend/entities/Reply"
          },
          "description": "Stores replies within a specific message thread.",
           "params": [
            {
              "name": "messageId",
              "description": "The ID of the parent message."
            },
            {
                "name": "replyId",
                "description": "The unique ID of the reply."
            }
          ]
        }
      }
    ]
  }
}
