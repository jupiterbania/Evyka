
/**
 * @fileoverview Firestore Security Rules for FocusFinds application.
 *
 * Core Philosophy:
 * This ruleset implements a hybrid security model, balancing public read access for certain data with strict owner-only or admin-only write access.
 * The user with the email 'jupiterbania472@gmail.com' is a designated super-admin with immediate privileges.
 *
 * Data Structure:
 * - /users/{userId}: Stores user profiles, accessible only to the user and admins.
 * - /users/{userId}/messages/{messageId}: User-submitted messages. Writable by the user, readable/manageable by admins and the user who created it.
 * - /media/{mediaId}: Stores media metadata, publicly readable but writable only by admins.
 * - /roles_admin/{userId}: Indicates admin privileges; presence of document grants access.
 * - /settings/main: A singleton document for site-wide settings, writeable only by admins.
 *
 * Key Security Decisions:
 * - Users can only read and write their own profile data and their own messages.
 * - Media metadata is publicly readable to enable browsing. Write access is restricted to admins.
 * - The `isDesignatedAdmin()` function grants immediate super-user access to avoid race conditions.
 * - The `settings/main` document is writeable by admins only, allowing for global site settings management.
 * - No user listing is allowed to prevent data scraping.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isExistingOwner(userId) {
      return isOwner(userId) && resource != null;
    }

    function isAdmin() {
      return isSignedIn() && exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid));
    }
    
    function isDesignatedAdmin() {
      return request.auth != null && request.auth.token.email == 'jupiterbania472@gmail.com';
    }

    match /users/{userId} {
      allow get: if isOwner(userId) || isAdmin() || isDesignatedAdmin();
      allow list: if false; // No user listing allowed.
      allow create: if isOwner(userId);
      allow update: if isExistingOwner(userId) && request.resource.data.id == resource.data.id;
      allow delete: if isExistingOwner(userId);

      // Users can only access message documents within their own user path.
      match /messages/{messageId} {
        allow read, write: if isOwner(userId);

        match /replies/{replyId} {
          allow read, write: if isOwner(userId);
        }
      }
    }

    // Admins can access any message.
    match /users/{userId}/messages/{messageId} {
        allow read, write: if isAdmin() || isDesignatedAdmin();

        match /replies/{replyId} {
             allow read, write: if isAdmin() || isDesignatedAdmin();
        }
    }

    match /media/{mediaId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin() || isDesignatedAdmin();
    }

    match /roles_admin/{userId} {
      allow get, create, delete: if isAdmin() || (isOwner(userId) && isDesignatedAdmin());
      allow list, update: if false;
    }

    match /settings/main {
      allow get: if true;
      allow create, update, delete: if isAdmin() || isDesignatedAdmin();
    }
  }
}
